// Package snd reads and writes MUGEN/Ikemen GO sound (.snd) files, v1 and
// v2, decoding to raw PCM. It has no playback dependency and compiles to
// WebAssembly.
package snd

// Version is the library version.
const Version = "0.3.1"
