# modules/root.md — package `snd`

**Role:** placeholder. Only `version.go` exists so far — the actual `.snd` v1/v2 parse/decode/serialize code has not been written yet.

**Files:** `version.go`

**Exports:** `Version` (string constant)
